%article.video_category
  %header
    %h3= link_to "#{@search_results.count.to_s} result(s)"
  .videos.row
    - @search_results.each do |video|
      .video.col-sm-2
        = link_to video_path(video) do
          =image_tag(video.small_cover_url)